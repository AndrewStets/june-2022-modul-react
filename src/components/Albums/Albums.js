import React, {useEffect, useState} from 'react';

const Albums = () => {

    const [albums,setAlbums] = useState([]);

    useEffect( ()=>{
        fetch()
    })

    return (
        <div>

        </div>
    );
};

export default Albums;